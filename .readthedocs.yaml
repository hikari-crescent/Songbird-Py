version: 2

formats: []

python:
  install:
    - requirements: docs/requirements.txt
    - requirements: requirements.txt
    - method: pip
      path: .
  system_packages: true

build:
  os: ubuntu-20.04
  tools:
    python: "3.8"
    rust: "1.55"
  apt_packages:
    - libopus-dev
